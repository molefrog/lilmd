# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# dev/experiment artifacts (corpus is big, fixtures are derived; results are
# small JSON and are committed so the report is reproducible-ish without
# rerunning the benches)
dev/corpus/
dev/fixtures/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
